apiVersion: apps/v1
kind: Deployment
metadata:
  name: mtafiti-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mtafiti-worker
  template:
    metadata:
      labels:
        app: mtafiti-worker
    spec:
      containers:
        - name: worker
          image: ghcr.io/mdudaj/mtafiti-backend:latest
          imagePullPolicy: IfNotPresent
          command: ["celery", "-A", "config", "worker", "-l", "INFO"]
          env:
            - name: DJANGO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: mtafiti-backend-secrets
                  key: DJANGO_SECRET_KEY
            - name: POSTGRES_DB
              value: mtafiti
            - name: POSTGRES_USER
              value: mtafiti
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mtafiti-backend-secrets
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_HOST
              value: postgres
            - name: POSTGRES_PORT
              value: "5432"
            - name: CELERY_BROKER_URL
              value: amqp://guest:guest@rabbitmq:5672//
            - name: RABBITMQ_URL
              value: amqp://guest:guest@rabbitmq:5672//
