# Task Tracking

- Issue #111: spec-first assessment for Sample Accession as a reference operation.
- Plan:
	- compare the `specs/006-sample-accession-reference-operation/` bundle against the current Sample Accession code, tests, and UI surfaces
	- determine whether `#111` is still one bounded follow-on issue or should be split into narrower implementation issues before more coding starts
	- capture the current conclusion from repository evidence: large parts of the reference bundle, runtime adapters, and proving UI already exist on `main`
	- recommend the next issue shape from the remaining gaps instead of treating `#111` as a clean implementation slice